Issue <%= @issue.issue_key || @issue.id %>: <%= @issue.title %>

Project: <%= @issue.project&.name || "Project ##{@issue.project_id}" %>
Status: <%= @previous_status && @previous_status != @issue.status ? "#{@previous_status} -> #{@issue.status}" : @issue.status %>
Assignee: <%= @previous_assignee && @previous_assignee != @issue.assignee ? "#{@previous_assignee} -> #{@issue.assignee.presence || 'Unassigned'}" : (@issue.assignee.presence || 'Unassigned') %>
Due date: <%= @issue.due_date&.strftime("%Y-%m-%d") || "Not set" %>

Description:
<%= @issue.issue_description.presence || "No description provided." %>

---
You are receiving this email because you are listed as the owner or assignee.
