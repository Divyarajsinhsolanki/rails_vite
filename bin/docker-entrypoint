#!/bin/bash -e

# Generic Docker platforms may not provide a release phase. Compose runs the
# dedicated release service and disables this fallback to avoid duplicate work.
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ] && [ "${AUTO_RELEASE:-true}" != "false" ]; then
  ./bin/release
fi

exec "${@}"
